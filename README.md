# To RUN jar file via docker-compose

we can run jar file file docker-compose.yml file

# To Run docker-compose.yml
docker-compose up -d
"-d" is used to send in background 
docker-compose ps 
to check running
